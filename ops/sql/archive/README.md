# Archived: ops/sql/001_mvo_schema.sql

Superseded by `supabase/migrations/20260715_000001_lautowork_control_core.sql`
(schema `lautowork`, org-aware via `platform.organizations`). Confirmed
never applied to any live database (VPS destroyed, old Supabase project
abandoned per Principal instruction — same status as LiNKsites' retired
`lsites_init.sql`). See `docs/archive/adr/0001-adopt-shared-platform-org-model.md`.
