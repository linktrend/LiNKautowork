import { describe, expect, it } from 'vitest'; import { admitProviderNotification, notificationReceipt } from '../src/services/provider-notifications.js';
describe('provider notifications', () => { it('enforces recipient, dedupe, suppression and rate limits', () => { const seen = new Set<string>(); const base = { seen, recipientAuthorized: true, suppressed: false, quiet: false, deliveredInWindow: 0, limit: 1, body: 'redacted note' }; expect(admitProviderNotification(base)).toBe('accepted'); expect(admitProviderNotification(base)).toBe('duplicate'); expect(admitProviderNotification({ ...base, body: 'new', suppressed: true })).toBe('suppressed'); expect(admitProviderNotification({ ...base, body: 'new', recipientAuthorized: false })).toBe('forbidden'); }); it('never returns content in receipt', () => expect(notificationReceipt('n1', 'failed', 2)).toEqual({ id: 'n1', state: 'failed', attempt: 2, redacted: true })); });
