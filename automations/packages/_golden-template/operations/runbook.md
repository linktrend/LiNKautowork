# Golden Example Runbook

This package is a structural example only. It must not be imported as a production automation or bound to a client.

An evaluator imports the inactive n8n workflow into a disposable test environment, executes the two declared cases, verifies the output contract, and records a redacted execution receipt. Any future release follows the normal candidate, independent-evaluation, and promotion gates.
