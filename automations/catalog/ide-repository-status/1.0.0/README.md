# IDE Repository Status 1.0.0

This exact, source-only canary package is manual and inactive. Its n8n graph contains only a manual trigger and a core Set node that returns bounded contract metadata; it has no credentials, network nodes, or external side effects.

`automation.json` binds the immutable identity to the contracts, workflow, evaluation suite, operating references, and provenance. The catalogue validator calculates and verifies both workflow and package digests before registration.
